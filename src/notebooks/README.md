## Notebooks

This folder is intended for exploratory analysis and evaluation notebooks.

At the moment, the historical notebook still lives at `src/agents/results.ipynb` (kept to avoid large-file churn during refactors).
If you want, you can later move it with a normal `git mv src/agents/results.ipynb src/notebooks/results.ipynb`.

